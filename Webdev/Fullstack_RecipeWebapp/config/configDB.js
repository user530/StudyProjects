const dbHost = `localhost`;
const userName = `root`;
const userPass = ``;
const baseName = `agile_project`;

const dbConfig = {
  host: dbHost,
  user: userName,
  password: userPass,
  database: baseName,
};

module.exports = {
  dbConfig: dbConfig,
};
